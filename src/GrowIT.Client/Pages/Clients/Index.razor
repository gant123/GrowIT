@page "/clients"
@attribute [Authorize]
@using GrowIT.Shared.DTOs
@using GrowIT.Shared.Enums
@using Syncfusion.Blazor.Grids
@using GrowIT.Client.Shared 
@inject HttpClient Http
@inject NavigationManager Nav

<div class="dashboard fade-in mt-4">
    <PageHeader Title="Case Files" Subtitle="Manage all active households and profiles.">
        <Actions>
            <button class="btn-growit btn-primary-growit shadow-sm" @onclick="OpenCreateModal">
                <span class="oi oi-plus me-2"></span>New Client
            </button>
        </Actions>
    </PageHeader>

    @if (clients == null)
    {
        <div class="loading-state">
            <div class="spinner-growit"></div>
            <p class="loading-state-text">Loading directory...</p>
        </div>
    }
    else
    {
        <div class="card-growit">
            <div class="card-body p-0">
                <SfGrid DataSource="@clients" 
                        AllowPaging="true" 
                        AllowSorting="true" 
                        AllowFiltering="true" 
                        Toolbar="@(new List<string>() { "Search" })"
                        Height="100%">
                    
                    <GridPageSettings PageSize="10"></GridPageSettings>
                    <GridFilterSettings Type="Syncfusion.Blazor.Grids.FilterType.Menu"></GridFilterSettings>
                    
                    <GridColumns>
                        <GridColumn HeaderText="Client Name" Width="240">
                            <Template>
                                @{
                                    var client = (context as ClientDto);
                                    <div class="d-flex align-items-center gap-3 cursor-pointer" @onclick="() => NavigateToClient(client.Id)">
                                        <div class="avatar-growit avatar-md">
                                            @GetInitials(client.Name)
                                        </div>
                                        <div class="overflow-hidden">
                                            <div class="fw-600 text-primary text-truncate">@client.Name</div>
                                            <div class="small text-muted text-truncate">@client.Email</div>
                                        </div>
                                    </div>
                                }
                            </Template>
                        </GridColumn>

                        <GridColumn Field=@nameof(ClientDto.LifePhase) HeaderText="Life Phase" Width="140" TextAlign="TextAlign.Center">
                            <Template>
                                @{
                                    var phase = (context as ClientDto).LifePhase;
                                    <span class="badge-growit @GetPhaseBadgeClass(phase)">
                                        @phase
                                    </span>
                                }
                            </Template>
                        </GridColumn>

                        <GridColumn Field=@nameof(ClientDto.StabilityScore) HeaderText="Stability" Width="160" TextAlign="TextAlign.Center">
                            <Template>
                                @{
                                    var score = (context as ClientDto).StabilityScore;
                                    
                                    <div class="px-2">
                                        <div class="d-flex justify-content-between small mb-1">
                                            <span class="fw-bold text-muted">Score</span>
                                            <span class="fw-bold">@score/10</span>
                                        </div>
                                        <div class="stability-bar">
                                            <div class="stability-fill" style="width: @($"{score * 10}%")"></div>
                                        </div>
                                    </div>
                                }
                            </Template>
                        </GridColumn>

                        <GridColumn Field=@nameof(ClientDto.Phone) HeaderText="Phone" Width="140"></GridColumn>

                        <GridColumn Width="100" TextAlign="TextAlign.Center">
                            <Template>
                                <button class="btn-growit btn-ghost-growit btn-sm" 
                                        @onclick="() => NavigateToClient((context as ClientDto).Id)">
                                    View File
                                </button>
                            </Template>
                        </GridColumn>
                    </GridColumns>
                </SfGrid>
            </div>
        </div>
    }
    
    <AddClientModal @bind-Show="showCreateModal" OnClientAdded="LoadClients" />

</div>

@code {
    private List<ClientDto>? clients;
    private bool showCreateModal = false;

    protected override async Task OnInitializedAsync() => await LoadClients();

    private async Task LoadClients()
    {
        try
        {
            clients = await Http.GetFromJsonAsync<List<ClientDto>>("api/clients");
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading clients: {ex.Message}");
        }
    }

    private void NavigateToClient(Guid id) => Nav.NavigateTo($"clients/{id}");
    private void OpenCreateModal() => showCreateModal = true;

    private string GetInitials(string name)
    {
        if (string.IsNullOrEmpty(name)) return "?";
        var parts = name.Split(' ', StringSplitOptions.RemoveEmptyEntries);
        if (parts.Length == 1) return parts[0].Substring(0, 1).ToUpper();
        return $"{parts[0][0]}{parts[^1][0]}".ToUpper();
    }

    private string GetPhaseBadgeClass(LifePhase phase) => phase switch
    {
        LifePhase.Crisis => "badge-phase-crisis",
        LifePhase.Stable => "badge-phase-stable",
        LifePhase.Thriving => "badge-phase-thriving",
        _ => "badge-neutral"
    };
}