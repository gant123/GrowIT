@page "/organization"
@inject NavigationManager Navigation

@* Organization configuration now lives in Settings -> Organization. This route is kept
   as a redirect so existing links/bookmarks don't 404. *@

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/settings", replace: true);
    }
}
